diff --git a/dist/index.mjs b/dist/index.mjs
index 9f103e46c4b79b25e5a51131763abeed6f6fce12..481a0a454f9380637f1c7bb54ef472fab7953450 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -1466,7 +1466,9 @@ var opts = [
   "wisp_ws_protocols",
   "redirect_limit",
   "header_limit",
-  "buffer_size"
+  "buffer_size",
+  "pem_files",
+  "disable_certificate_validation"
 ];
 var EpoxyTransport = class {
   ready = false;
@@ -1495,16 +1497,42 @@ var EpoxyTransport = class {
   async request(remote, method, body, headers, signal) {
     if (body instanceof Blob)
       body = await body.arrayBuffer();
+    const _tag = `[epoxy] ${method} ${remote.href}`;
+    console.log(`${_tag} START`);
     try {
       let res = await this.client.fetch(remote.href, { method, body, headers, redirect: "manual" });
+      console.log(`${_tag} fetch RESOLVED status=${res.status}`);
+      let wrappedBody = res.body;
+      if (wrappedBody instanceof ReadableStream) {
+        const origBody = wrappedBody;
+        wrappedBody = new ReadableStream({
+          start(controller) {
+            const reader = origBody.getReader();
+            (async () => {
+              try {
+                while (true) {
+                  const { done, value } = await reader.read();
+                  if (done) break;
+                  controller.enqueue(value);
+                }
+                controller.close();
+                console.log(`${_tag} body DONE`);
+              } catch (streamErr) {
+                console.error(`${_tag} body STREAM ERROR:`, streamErr);
+                try { controller.error(streamErr); } catch (_) {}
+              }
+            })();
+          }
+        });
+      }
       return {
-        body: res.body,
+        body: wrappedBody,
         headers: res.rawHeaders,
         status: res.status,
         statusText: res.statusText
       };
     } catch (err) {
-      console.error(err);
+      console.error(`${_tag} REQUEST ERROR:`, err);
       throw err;
     }
   }
